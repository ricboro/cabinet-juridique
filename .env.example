# Générer avec : python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changez-moi-avec-une-vraie-cle-secrete

# Identifiants du premier compte avocat (créé au premier démarrage)
ADMIN_EMAIL=avocat@cabinet.fr
ADMIN_PASSWORD=changez-moi

# Chemin de la base SQLite (ne pas modifier sauf si hors Docker)
DATABASE_URL=sqlite:////data/cabinet.db

PORT=8092

# Clé d'authentification API REST (Tailscale-only, header X-API-Key)
# Générer avec : python3 -c "import secrets; print(secrets.token_hex(32))"
API_KEY=
